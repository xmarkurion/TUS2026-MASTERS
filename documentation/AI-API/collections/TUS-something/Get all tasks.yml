info:
  name: Get all tasks
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/tasks/findAllTasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
