info:
  name: assign taks
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/tasks/agents/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
