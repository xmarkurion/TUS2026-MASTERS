info:
  name: SEND
  type: http
  seq: 1

http:
  method: post
  url: https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent
  headers:
    - name: x-goog-api-key
      value: "{{process.env.key}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "contents": [
          {
            "parts": [
              {
                "text": "Get me funny joke fast and not much lines."
              }
            ]
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
