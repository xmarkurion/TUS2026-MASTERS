info:
  name: Get All models
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:1234/api/v1/models
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
