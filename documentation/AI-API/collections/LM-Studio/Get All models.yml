info:
  name: Get All models
  type: http
  seq: 2

http:
  method: GET
  url: |-
    /api/v1/models


    /api/v1/models

    http://localhost/api/v1/models
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
