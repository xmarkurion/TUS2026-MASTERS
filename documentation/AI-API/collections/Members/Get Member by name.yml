info:
  name: Get Member by name
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/members/findByName?name=kim
  params:
    - name: name
      value: kim
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
