info:
  name: Delete Memeber
  type: http
  seq: 2

http:
  method: DELETE
  url: http://localhost:8080/members/delete/{{id}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: u2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
